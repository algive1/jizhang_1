# 验收标准

- [x] 一级、二级分类种子、ID、名称、顺序和存储关联保持现状。
- [x] 二级分类弹层中没有“不细分”文本、parent tile 或 footer 入口。
- [x] 二级裸图标仍为28px主题色，指针滑动时邻近图标放大并随动，松手选中原分类 ID。
- [x] 普通点击只选择一项；多项列表仍可滚动，滚动操作不误选。
- [x] liquidGlass 主题下的弹层仍使用 BackdropFilter 模糊与主题色玻璃质感。
- [x] 点击记一笔的页面转场顺畅完成；系统返回先关闭浮层、再关闭记一笔并完成反向动画。
- [x] 普通页面 Android edge swipe 返回测试通过；fullscreenDialog 按设计仍是模态，不触发 edge swipe。
- [x] 相关 widget 测试通过；scope analyze 无问题；diff check 通过；Debug APK 构建通过。
- [x] 最新 APK 安装到真机并启动，现场检查记一笔浮层与动画。

## 二级浮层对比度补充验收（用户 2026-09-23 最新反馈）

- [x] 浮层打开后，外部整页遮罩明显压暗背景（barrier alpha 至少 0.55），焦点落在浮层。
- [x] Liquid Glass 浮层使用 blur sigma 至少 30、玻璃层覆盖度至少 0.94；最新主题截图中背景分类图标/文字不可辨认，浮层内二级图标与名称清楚。
- [x] 非 Liquid Glass 主题的浮层背景完全不透明并使用对应主题表面色。
- [x] 全局其他 AppGlassSurface 默认外观、二级分类内容/图标主题色、跟手放大手势、点击和滚动行为均不变。
- [x] 新增视觉配置断言先 RED 后 GREEN；quick-add 相关测试、scope analyze、diff-check 通过。
- [ ] 最新 APK 重建并安装到 Xiaomi 23116PN5BC；现场查看以上对比度要求及返回行为。

# 页面转场旧内容穿透验收（2026-09-23）

- [x] push 转场中途：新页面空白区的实际像素为当前主题底色，旧页面文字或颜色不穿透。
- [x] 侧滑返回中途：新页面剩余区域的空白区不穿透旧页面内容；页面仍随手指移动。
- [x] 记一笔 fullscreenDialog 进入期间有不透明页面底层，相关记一笔组件测试通过。
- [x] 主 Shell 的 mesh 背景与底部液态玻璃组件绘制顺序保留，玻璃组件测试通过。
- [x] 本次改动定向测试与相关导航/记一笔测试、范围 analyze、Debug APK build、`git diff --check` 通过。
- [x] 指挥层检查 `git diff`、`git status`；测试生成的五张 QA 截图已还原，既有未跟踪文件未改动。
- [x] Debug APK 已在 Xiaomi 23116PN5BC 安装并打开；液态玻璃主题首页玻璃导航与记一笔静止画面已复核，卡片空隙无旧页文字。
- [ ] 真机逐帧动态检查转场与 Impeller 折射；连续截图未捕获中途帧，设备随后切至其他前台应用，已停止操作。

# 液态玻璃导航栏选中态验收（2026-09-23）

- [x] 液态玻璃普通对比度模式的导航胶囊使用浅白透明底，不再使用暗化 `plateTint`。
- [x] 移动选中层具有低透明度的当前主题色；按液态玻璃主题页面底色合成，其与栏体的计算对比度至少 1.15:1；静止选中层仍为可辨的淡主题色，动画和几何参数不变。
- [x] 选中图标与文字颜色不被提亮，计算对比度在选中层上至少 4.5:1。
- [x] 其他三套主题和高对比度模式的原有颜色分支保持不变。
- [x] 新测试先 RED 后 GREEN；导航相关 widget 测试、范围 analyze、Debug APK build、`git diff --check` 通过。
- [x] 指挥层独立审阅 `git diff` 与 `git status`，确认无任务外代码变更或阻断级回归。

## 导航选中态全主题二次调整（2026-09-23）

- [x] 普通主题静止选中底色透明度从 `.26` 降至 `.18`，四个内置主题均仍有可辨淡主题色。
- [x] Liquid Glass 移动胶囊不再额外覆盖主题色，使用与其他主题相同的默认透明玻璃配置。
- [x] 选中图标与文字对比度至少 4.5:1；高对比度固定填充不变。
- [x] 导航和间隙 widget 测试、范围 analyze、Debug APK build、`git diff --check` 通过。
- [x] Debug APK 成功安装到 Xiaomi 23116PN5BC 并启动。

## 选中图标明度微调（2026-09-23）

- [x] 共享选中图标/文字颜色亮度系数从 `.80` 提至 `.85`。
- [x] 四套主题选中 ink 对各自淡选中底色的 WCAG 对比度均不低于 4.5:1；未选中 ink 不变。
- [x] 导航/布局测试、scope analyze、Debug APK build、`git diff --check` 通过。
- [x] 更新后的 APK 成功安装到 Xiaomi 23116PN5BC 并启动。

## 木木参考下的选中态层次修正（2026-09-23）

- [x] 记录木木反编译代码中浅色静止 pill 的实际颜色及选中图文颜色来源，说明版本差异和可复用范围。
- [x] 液态玻璃静止选中背景比当前 `.18` 主题色明显更淡，轮廓仍可辨；其他浅色主题颜色协调，移动玻璃继续透明。
- [x] 选中图标和文字在四套主题均较当前 `.85` 样式更鲜亮，同时对实际静止 pill 背景的计算对比度均至少 4.5:1；未选中项和高对比度模式不退化。
- [x] 真机截图或直接查看证明灰蓝整块感减轻，导航仍能明确指出选中项。
- [x] 导航相关测试、范围 analyze、Debug APK build、`git diff --check` 通过；APK 已重新安装并启动到 Xiaomi 23116PN5BC。

## FAB 与导航栏统一玻璃材质（2026-09-23）

- [x] 四套内置主题均使用现有 `LiquidGlassTabBar` 导航和 `LiquidGlassFab`，FAB 玻璃底与该主题导航胶囊同色，Liquid Glass 为白色 alpha `.24`。
- [x] FAB 加号使用该主题的导航选中强调色，四主题颜色各异；玻璃底上非文本对比度至少 3:1。
- [x] FAB 折射、模糊、光学边缘、阴影、尺寸、点击和长按功能保留；高对比度分支仍为不透明按钮。
- [x] 新测试先 RED 后 GREEN；FAB/导航相关测试（25 项）、范围 analyze、Debug APK build、`git diff --check` 通过。
- [x] 指挥层审查 `git diff` 和 `git status`，四主题样式与对比度断言通过；Debug APK 安装到 Xiaomi 23116PN5BC 返回 `Success`。

## 四主题统一采用液态玻璃导航样式（2026-09-23 用户反馈修正）

- [x] 四套普通主题的导航胶囊背景均为白色 alpha `.24`，选中静止 pill 均为白色 alpha `.10`；三套固体主题没有主题色选中底。
- [x] 四套普通主题的 FAB 玻璃底均与导航一致为白色 alpha `.24`；加号仍使用相应主题的选中强调色。
- [x] 四主题导航选中/未选中文字对比度至少 4.5:1，FAB 加号对玻璃底至少 3:1；高对比度与移动玻璃配置不回退。
- [x] 新颜色回归先 RED 后 GREEN；导航、FAB 和布局相关测试（25 项）、范围 analyze、Debug APK build 与 `git diff --check` 通过。
- [x] 指挥层审阅当前 diff/status；新 APK 已安装到 Xiaomi 23116PN5BC，包管理器确认 APK 路径存在。

## 深灰静止胶囊网页预览（2026-09-23）

- [x] 四主题普通模式静止选中胶囊均为 `#333333` alpha `.10`；移动玻璃仍透明。
- [x] 导航/FAB 测试通过，四主题选中和未选中文字对比度均至少 4.5:1。
- [x] 范围静态分析、Debug APK build 和 `git diff --check` 通过。
- [x] 可切换四主题的网页预览已生成并可由用户本地打开。
- [x] 本轮没有操作或切换真机前台。

## 静止胶囊 3% 真机复核（2026-09-23）
- [x] 四主题静止胶囊为深灰 `#333333` alpha `.03`，测试锁定该值。
- [x] 导航/FAB/布局定向测试 25 项通过，选中和未选中 ink 对比度断言通过。
- [x] 范围 `flutter analyze`、Debug APK 构建及 `git diff --check` 通过。
- [x] 网页预览样式和文案已同步为 3%。
- [x] Debug APK 安装至 Xiaomi 23116PN5BC 返回 `Success`，启动命令已执行。

# 二级分类浮层导航同款玻璃与紧凑布局

- [x] Liquid Glass 主题的二级浮层使用导航同款透明液态玻璃 tint（白色 alpha `.24`）、导航层高光及背景模糊。
- [x] 其他主题的二级浮层仍使用不透明主题表面色。
- [x] 默认浮层宽度不超过 344 dp、分类单格基础高度 58 dp、普通字号五列；大字号仍四列且安全区内可滚动。
- [x] 一级分类“其他”固定第一；其余按现有 sortOrder 倒序；分类数据、ID、名称和持久化 sortOrder 不变。
- [x] 液态玻璃浮层与选中胶囊复用导航白色 alpha `.24` 和 5 dp 背景模糊，按压/滑动时胶囊移动，其他主题保持不透明表面。
- [x] 点击选择、指针滑动选择、长列表滚动和外部/系统返回行为无回归。
- [x] 新增/相关 widget tests（28 项）、范围 analyze、Debug APK build、`git diff --check` 通过。
- [x] 最新 Debug APK 安装至 Xiaomi 23116PN5BC；`install -r` 返回 `Success`，包路径有效，进程 PID `28857`，前台为 `com.algive.jizhang_app/.MainActivity`。


## 二级分类弹窗亮度与中性玻璃修订（2026-09-23）

- [x] 弹层外仍保留用于聚焦的黑色页面遮罩（alpha 至少 0.55），外部点击仍能关闭。
- [x] Liquid Glass 二级弹窗使用中性白玻璃覆盖度 0.94、导航同款 5 dp 背景模糊；屏幕遮罩不会让弹窗本体发暗。
- [x] 二级弹窗及移动选中胶囊关闭主题色渐变、阴影及彩色边缘；其他 AppLiquidGlassSurface 调用方默认外观保持不变。
- [x] 移动选中胶囊动画、点击/滑动/滚动选择、一级分类顺序和其他主题表面无回归。
- [x] 相关 widget tests（28 项）、scope analyze、Debug APK build、git diff --check 通过。
- [x] 最新 APK 安装到 Xiaomi 23116PN5BC 并启动；`install -r` 返回 `Success`，包路径有效，PID `5691`，前台为 `com.algive.jizhang_app/.MainActivity`。
# 2026-09-24 五项改动验收

- [x] 微信/支付宝各展示入口与会员支付方式使用同一图片资产；非品牌账户图标保留现有语义，图片在不同尺寸下不变形。
- [x] 消费日历“本月概览”固定在页面底部；滚动日期和当日流水时始终可见；SafeArea、窄屏/大字无遮挡、无溢出。该页面本身没有输入框；键盘出现时由 Scaffold 默认避让行为处理。
- [x] 首页、主流水、搜索、消费日历、账户详情、资产最近变动及其他真实流水列表可长按打开现有操作层；点击仍进入详情，操作后数据刷新。
- [x] 首页目标卡可点击进入目标列表或当前目标详情；预算入口、隐藏金额、目标内部控件仍各自正常。
- [x] 成功导入新流水后，目标建议和洞察基于新数据重算；全重复/失败导入不误报成功，也不使用旧统计；跨账本范围正确。
- [x] 关键新增/更新的定向测试观察到预期 RED 后 GREEN；相关 Flutter 测试、范围 analyze、Debug APK 构建及 `git diff --check` 通过。全仓 analyze 的唯一提示来自任务前已有的未跟踪 `test/zz_tmp_render_shots_test.dart:4`，不属于本次改动。
- [x] 指挥层独立审核 `git diff`/`git status`，确认未覆盖任务前已有未提交改动且无阻断回归。
- [x] 跨账本日历/资产记录长按时，当前账本的删除、退款、报销等写入动作不可直接执行；用户切换到流水所属账本后可打开完整操作层，同账本行为不变。

## 2026-09-24 消费日历卡片背景修正

- [x] 本月概览仍由独立底栏卡片固定，底栏包装不绘制不透明背景。
- [x] Scaffold body 延伸至固定卡片背后，滚动内容经过底栏区域；卡片和 SafeArea 保持可见。
- [x] 日历页面范围 `flutter analyze --no-pub` 与目标文件 `git diff --check` 通过。

## 2026-09-24 首页分类流水弹层与多指标趋势

- [x] 首页分类点击后，弹层流水与分类统计采用同一分类键规则，展示周期范围内、当前账本、CNY、未删除且符合分类统计当前 `isExpense` 支出口径的记录（包括资产支出）。
- [x] 右侧提供“日期”和“金额”两个主题化排序按钮；默认日期倒序，点击当前项切换方向，切换字段后显示对应顺序；详情和长按操作仍可用。
- [x] 弹层总高度不超过屏幕高度的 1/2；标题/排序区固定，流水区超出时可滚动。
- [x] 趋势图保留周/月/年范围与点选交互，三条线分别表示退款后支出、收入、总资产；支出用主题色、收入用现有绿色、资产用橙色。
- [x] 每条趋势的选中金额均可读且为账务净额；月视图按月汇总收入/支出、显示月末总资产；总资产汇总正向账户余额并计入投资市值，不扣除负债。
- [x] `CashflowTrendChart` 分析页面现有用法默认表现不变；本次目标文件范围 `flutter analyze --no-pub`、`git diff --check` 与 Debug APK build 通过。按更高优先级指示，不新增或运行测试。
- [x] 独立审查 `git diff` 与 `git status`；原有未提交修改完整保留，无阻断回归。

## 2026-09-24 消费日历、反馈提示与统一日期时间样式

- [x] 消费日历滚动到最大偏移时，最后一条消费记录和“记一笔”入口不被“本月概览”遮挡。
- [x] 320dp、字号 1.8 的消费日历无布局溢出；“本月概览”仍固定可见并可交互。
- [x] 记一笔完成、部分成功和校验反馈均使用主题化浮动提示；提示不被导航栏或 FAB 覆盖，背景不再为默认黑色。
- [x] `lib/` 业务页面不再直接调用 `showDatePicker`/`showTimePicker`；纯日期入口统一走 `AppDatePicker`，日期+时间入口走 `TimeSelector`。
- [x] 所有被替换日期入口保留原始最小/最大日期范围、初始值和保存语义。
- [x] 新增回归测试先观察 RED 再观察 GREEN；37 项主回归测试、7 项日期组件兼容测试通过；14 个目标文件 analyze 无问题；Debug APK build 与 `git diff --check` 通过。
- [x] 指挥层独立复核 `git diff`、`git status`，确认既有未提交改动未被覆盖且无阻断级回归；两轮独立只读代码审查均 PASS。

## 2026-09-24 资产总览分布与投资计入首页开关

- [x] 新投资持仓的“计入首页账目净资产”默认关闭；开关值持久化，重载后保持原值。
- [x] v21 数据库升级后，原投资持仓仍存在、投资管理总值不变，新 inclusion 列值为 false。
- [x] 关闭的投资持仓仍显示在投资管理完整组合中；首页资产卡、首页趋势、资产总览合计和分布不计入其市值；开启的持仓按币种计入。
- [x] 资产分布与资产变化卡在 320dp、393dp 可用宽度均同一行显示，无 RenderFlex/viewport 异常。
- [x] 分布 donut 与详情使用相同的账户和投资数据；投资管理行显示金额和总资产百分比；各详情行金额占比与总计一致，零资产无除零异常。
- [x] 资产变化卡与详情单独显示当前计入投资市值，区间趋势继续按账本历史计算，不伪造投资历史收益。
- [x] 点资产分布、资产变化卡均进入对应详情；弹层无黑色条纹、内容可滚动、金额/占比排版对齐。
- [x] 新增投资页默认关闭；点击提示控件显示完整文案“关闭后，投资类金额仅在投资管理页面展示，不计入首页展示的账目净资产。”
- [x] RED→GREEN 目标测试通过；目标文件 `flutter analyze --no-pub` 无问题；Debug APK 构建成功；`git diff --check` 退出码为 0。
- [x] 指挥层独立检查 `git diff` 和 `git status`，确认开工前所有用户未提交改动完整保留。
