# 阶段十代码资产审查

审查时间：2026-08-31

## 审查范围

- 当前项目：`/Users/algive/jizhang_01`
- 用户目录下深度 4 以内、排除 `Library`、`.pub-cache`、`.codex` 和构建依赖目录的工程清单
- 关键词：user/auth、membership/vip、entitlement/subscription、order/payment、wechat/apple、object storage/upload、config/admin、sync/cloud

## 结论

当前可访问文件中未发现文档所述“其他项目”的用户、会员、支付、对象存储、云同步或后台管理源码。未经源码、接口协议和服务端配置审查，不能迁移或重建真实商业链路。

| 资产 | 分级 | 审查结论 |
| --- | --- | --- |
| 用户系统 | C | 未提供旧资产；当前仅有本地用户标识，不等同于认证系统 |
| 会员 | C | 未提供旧资产；只能先建立 `Membership` 领域契约 |
| 权益 | C | 未提供旧资产；客户端可建立 `Entitlement` 消费接口，最终授权必须来自服务端 |
| 订单 | C | 未提供旧资产；不创建假订单或固定成功结果 |
| 微信支付 | C | 未提供商户配置、服务端下单、回调验签代码，不能联调 |
| Apple 支付 | C | 未提供 StoreKit 商品和服务端票据校验代码，不能联调 |
| 对象存储 | C | 未提供供应商、签名上传协议和 bucket 策略，不能联调 |
| 文件上传 | B | 当前本地附件复制逻辑可保留；云上传需接旧项目签名协议后改造复用 |
| 配置系统 | B | 当前 `AppSettingsRepository` 只适合本机偏好；商业配置必须接服务端配置源 |
| 后台管理 | C | 未提供旧资产，客户端不能替代后台权限和运营管理 |

## 当前允许实施的范围

- 建立 `Membership`、`Subscription`、`Entitlement`、`UsageQuota` 类型和 Repository 接口。
- 所有页面只从权益快照判断能力，不使用 `user.vip = true`。
- 建立支付、对象存储、云同步抽象；未配置时明确返回“不可用”，不伪造成功。
- 会员页展示 Free / Pro / Family 价值，但在真实支付链路接入前不开放购买。

## 继续真实联调所需输入

- 旧项目绝对路径或源码包。
- 用户/会员/订单/权益 API 协议与测试环境。
- 微信支付商户及服务端回调环境；Apple IAP 商品与服务端票据验证环境。
- PostgreSQL 同步协议、对象存储签名上传协议、数据保留策略。
