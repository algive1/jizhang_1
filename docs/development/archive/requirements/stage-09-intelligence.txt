现在开发：

Transaction Classification
+
Deduplication

基础引擎。

先不接 AI。

==================================================
一、分类优先级
==================================================

分类顺序：

1. 用户个人规则
2. 精确商户规则
3. 关键词规则
4. 默认分类
5. 待确认

==================================================
二、Personal Merchant Memory
==================================================

例如：

用户把：

雅迪

改为：

交通 → 电动车

以后：

相同商户优先使用用户修正结果。

建立：

MerchantRule

merchantPattern
categoryId
subcategoryId
userId
confidence
source
createdAt
updatedAt

==================================================
三、分类修改记录
==================================================

当用户修改自动分类：

userCorrected = true

同时更新个人规则。

但用户可以选择：

仅修改本次
以后都这样分类

==================================================
四、交易去重
==================================================

建立 TransactionFingerprintService。

判断：

金额
时间
商户
账户
来源
支付渠道
银行卡尾号
订单号（如果有）

当前没有完整自动账单来源时，
先实现基础接口和规则。

==================================================
五、疑似重复
==================================================

不要直接删除。

状态：

duplicateConfidence

例如：

> 0.98

如果非常高：

合并候选。

如果中等：

进入待确认。

==================================================
六、同一经济事件
==================================================

例如：

支付宝付款 ¥38
+
招商银行扣款 ¥38

不能算两笔消费。

未来数据结构需要支持：

EconomicEvent

一笔经济事件
可能对应多个原始来源记录。

当前阶段至少把接口和模型设计好。

不要为了暂时简单，把去重写死成：

amount + minute

==================================================
七、待确认箱
==================================================

增加一个：

账单收件箱 / 待确认

状态：

分类不确定
疑似重复
缺少账户

当前可以使用本地数据模拟。

==================================================
八、测试
==================================================

重点测试：

同金额不同商户
同金额相近时间
支付宝+银行卡
用户手工新增相同金额

避免错误合并。